name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
       - uses: actions/checkout@v3

       # Replace the unavailable action with an alternative that updates profile README
       - uses: teoxoy/profile-readme-stats@latest
        with:
        USERNAME: AmmarYasser455
        COMMIT_MSG: 'Updated README with latest stats'
